// Default history size.
export const DEFAULT_HISTORY_SIZE = 100;

// Maximum time in milliseconds that the system recognizes as consecutive plays.
export const CONSECUTIVE_PLAY_THRESHOLD_MILLIS = 60 * 60 * 1000;
